[build]
  command = "pnpm build && mkdir -p netlify/functions && sed 's|../../../dist/server|../../dist/server|' .netlify/v1/functions/server.mjs > netlify/functions/server.mjs"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"
